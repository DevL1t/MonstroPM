# конфиг с секретами — не коммитим (создаётся мастером setup.php)
config.php
config.php.tmp

# правила планировщика + ключ крона — пользовательские данные
rules.json
rules.json.tmp

# логи, временное, дампы
*.log
/backups/
.claude/
*.sql

# служебные заметки для Claude — не в публичный репозиторий
CLAUDE.md


mpm.zip